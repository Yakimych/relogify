let containerStyle = ReactDOMRe.Style.make(~width="550px", ());

let numberCellStyle =
  ReactDOMRe.Style.make(
    ~width="20px",
    ~paddingLeft="10px",
    ~paddingRight="0",
    (),
  );

let playerLinkStyle =
  ReactDOMRe.Style.make(
    ~width="550px",
    ~textDecoration="none",
    ~color="rgba(0, 0, 0, 0.87)",
    (),
  );
